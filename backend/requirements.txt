fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.4.2
python-dotenv==1.0.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
alembic==1.13.0
requests==2.31.0
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.1

# Embedding/Search options (at least one recommended)
# Option 1: OpenAI (requires API key, has usage costs)
openai==1.3.5

# Option 2: Free local embeddings (recommended, no API key needed)
sentence-transformers==2.2.2

# Vector database support
pgvector==0.2.4
numpy==1.26.2

# Optional: Redis for caching (improves performance)
# redis==5.0.1